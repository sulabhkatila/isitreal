[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "isitreal"
version = "0.1.1"
description = "Dependency reality-checker for AI coding agents to verify packages before installing"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "Sulabh Katila", email = "katilasulabh@gmail.com" }
]
keywords = [
    "ai",
    "agent",
    "security",
    "dependencies",
    "slopsquatting",
    "hallucination",
    "mcp",
    "pypi",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Security",
    "Topic :: Software Development :: Quality Assurance",
]

dependencies = [
    "httpx>=0.27.0",
    "pydantic>=2.0.0",
    "click>=8.0.0",
    "rich>=13.0.0",
    "mcp>=1.0.0",
    "packaging>=23.0",
]

[project.urls]
Homepage = "https://github.com/sulabhkatila/isitreal"
Repository = "https://github.com/sulabhkatila/isitreal"
Issues = "https://github.com/sulabhkatila/isitreal/issues"

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "respx>=0.21.0",
    "pytest-cov>=5.0.0",
    "pytest-asyncio>=0.23.0",
]

[project.scripts]
isitreal = "isitreal.cli:main"
isitreal-mcp = "isitreal.mcp_server:main"

[tool.hatch.build.targets.wheel]
packages = ["isitreal"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
addopts = "-v"
